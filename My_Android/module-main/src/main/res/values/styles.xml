<resources xmlns:tools="http://schemas.android.com/tools">
    <!--StartActivity Style 冷启动效果-->
    <style name="AppTheme.Start">
        <item name="android:windowBackground">@drawable/activity_start</item>
    </style>

    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar" tools:ignore="ResourceName">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppSplashTheme" parent="@style/BaseAppTheme" tools:ignore="ResourceName">
        <!--<item name="android:windowBackground">@drawable/splash_bg</item>-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
